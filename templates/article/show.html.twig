<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{{ article.title }}</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                line-height: 1.6;
                margin: 0;
                padding: 20px;
                max-width: 800px;
                margin: 0 auto;
            }
            h1 {
                border-bottom: 1px solid #ddd;
                padding-bottom: 10px;
            }
            .article-content {
                margin-top: 20px;
                padding: 15px;
                background-color: #f9f9f9;
                border-radius: 4px;
                line-height: 1.8;
            }
            .back-link {
                display: inline-block;
                margin-top: 20px;
                padding: 8px 12px;
                background-color: #e7f3ff;
                color: #0066cc;
                border-radius: 4px;
                text-decoration: none;
            }
            .back-link:hover {
                background-color: #d0e8ff;
                text-decoration: underline;
            }
        </style>
    </head>
    <body>
        <h1>{{ article.title }}</h1>
        
        <div class="article-content">
            {{ article.content|nl2br }}
        </div>
        
        <a href="{{ path('article_list') }}" class="back-link">Back to all articles</a>
    </body>
</html>